server:
  port: 11001


security:
  oauth2:
    client:
      clientId: SampleClientId
      clientSecret: secret
      accessTokenUri: http://localhost:11000/oauth/token
      userAuthorizationUri: http://localhost:11000/oauth/authorize
    resource:
      userInfoUri: http://localhost:11000/user/me

debug: false

spring:
  application:
    name: ui
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
    encoding: UTF-8